{
  "name": "@monorepo/server",
  "version": "0.1.0",
  "main": "src/main.ts",
  "license": "MIT",
  "scripts": {
    "prebuild": "rimraf dist",
    "build": "tsc",
    "dev": "ts-node-dev --inspect --clear --transpile-only --no-notify -r tsconfig-paths/register src/main.ts",
    "lint": "echo >> /dev/null"
  },
  "dependencies": {
    "express": "^4.17.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.13",
    "@types/node": "^16.10.3",
    "rimraf": "^3.0.2",
    "ts-node-dev": "^1.1.8",
    "tsconfig-paths": "^3.11.0"
  }
}
